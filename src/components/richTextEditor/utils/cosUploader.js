// import { YxwCosUploader } from '@tencent/yunxiaowei-cos-v5';
// 原逻辑
// export function uploadFileByCos(file, name) {
  // return YxwCosUploader.getInstance({
  //   logger,
  //   request: Vue.$CMSAxios,
  // }).then(uploader => uploader.uploadFileByCos(file, name))
  //   .then(res => ({ data: res }));
// }

export function uploadFileByCos() {
  // 自定义自己的逻辑
}
